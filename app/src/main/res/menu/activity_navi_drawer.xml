<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_chart"
            android:icon="@drawable/ic_bar_chart"
            android:title="@string/bar_chart_view" />
        <item
            android:id="@+id/nav_list"
            android:icon="@drawable/ic_list_view"
            android:title="@string/list_view" />
        <item
            android:id="@+id/nav_pie_chart"
            android:icon="@drawable/ic_pie_chart"
            android:title="@string/pie_chart_view" />
    </group>

    <item android:title="@string/others">
        <menu>
            <item
                android:id="@+id/nav_weather"
                android:icon="@drawable/ic_sun"
                android:title="@string/weather_forecast" />
            <item
                android:id="@+id/nav_send"
                android:icon="@drawable/ic_menu_send"
                android:title="@string/reserved" />
        </menu>
    </item>

</menu>
